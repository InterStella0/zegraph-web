// Shared between the client sidebar and the /servers server layout; must stay
// in a module without 'use client' so the server can import the plain string.
export const COMMUNITY_COLLAPSE = "community"
