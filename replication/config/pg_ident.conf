# User name maps for the replica (installed to $CONFDIR/pg_ident.conf).
# Intentionally empty — it exists only because ident_file in postgresql.conf
# points here, and Postgres requires the referenced file to exist.
# MAPNAME  SYSTEM-USERNAME  PG-USERNAME
